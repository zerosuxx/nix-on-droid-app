<?xml version="1.0" encoding="utf-8"?>
<!-- Resources ported from the Termux:API app (termux-api) for the built-in API support.
     Only the subset referenced by the ported sources and manifest entries is included;
     the standalone Termux:API main/settings activity resources were intentionally left out. -->
<resources>

    <string name="share_file_chooser_title">Share with</string>
    <string name="grant_permission">Grant permission</string>
    <string name="keep_alive_service">This service keeps the API implementation running in the background for faster startup of termux-* commands.</string>
    <string name="permission_description">This app needs the following permission(s):\n</string>

    <color name="black_overlay">#66000000</color>

    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="windowNoTitle">true</item>
        <item name="bottomSheetDialogTheme">@style/Theme.Design.Light.BottomSheetDialog</item>
    </style>

    <style name="DialogTheme.Dark" parent="Theme.AppCompat.Dialog.Alert">
        <item name="windowNoTitle">true</item>
        <item name="bottomSheetDialogTheme">@style/Theme.Design.BottomSheetDialog</item>
    </style>

    <style name="TransparentTheme" parent="@style/Theme.AppCompat.Light">
        <item name="android:background">@android:color/transparent</item>
        <item name="background">@android:color/transparent</item>

        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:color/transparent</item>
    </style>

</resources>
